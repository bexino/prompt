# 仓库维护规范

## 结构规范

- `docs/spec.md` 是本项目文件结构、模块边界、文件职责、加载顺序、依赖关系和公开接口的唯一规范来源。
- 开始任何代码修改前，必须先浏览 `docs/spec.md` 中与当前任务相关的部分，确认修改应归属的层级和文件。
- 新增、删除、移动、重命名文件或目录时，必须在同一次修改中同步更新 `docs/spec.md`。
- 改变模块职责、依赖关系、加载顺序或公开接口时，必须在同一次修改中同步更新 `docs/spec.md`。
- 即使文件路径不变，只要文件的主要职责发生变化，也必须更新 `docs/spec.md`。
- 修改完成后必须核对实际目录、资源引用和脚本加载顺序是否与 `docs/spec.md` 一致。

## 语言规范

- 所有自行编写或修改的 HTML、CSS 和 JavaScript 注释一律使用简体中文。
- 所有自然语言 Markdown 内容一律使用简体中文，包括标题、正文、表格、说明、示例文字和维护信息。
- 代码标识符、文件名、路径、命令、API 名称、协议字段和无法合理翻译的专有名词保持原样。
- `LICENSE`、字体许可证及其他第三方授权原文不得翻译或擅自修改。
- 不为显而易见的代码添加冗余注释；注释应说明设计原因、边界条件或非直观行为。

## 修改原则

- 保持原生 HTML、CSS 和 JavaScript 架构，不引入框架、包管理器或构建工具，除非任务明确要求并同步修订结构规范。
- 可复用组件不得依赖主应用状态、固定业务数据源或 Tailwind 工具类。
- 素材访问优先经过 `js/config/asset-manifest.js`；CSS 中不可动态解析的字体和背景资源集中保留在对应样式文件，不得在业务脚本中散落路径。
- 新功能按照 `docs/spec.md` 的归属判断规则放置，禁止重新向 `index.html` 填入内联业务样式、内联业务脚本或内联事件。
